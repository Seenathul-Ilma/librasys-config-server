server:
  port: 8082

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: book_service_db

servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB

#gcs:
  #bucket-name: my-book-covers-bucket