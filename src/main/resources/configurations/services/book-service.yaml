server:
  port: 8082

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: librasys_book_service_db

servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB

gcs:
  bucket-name: librasys-book-covers